apiVersion: apps/v1
kind: Deployment
metadata:
  name: 
  namespace: 
  labels:
    app: 
    tenant: team-b
spec:
  replicas: 4
  selector:
    matchLabels:
      app: 
      tenant: team-b
  template:
    metadata:
      labels:
        app: 
        tenant: team-b
    spec:
      containers:
        - name: 
          image: 
          ports:
            - containerPort: 
              name: 
---
apiVersion: v1
kind: Service
metadata:
  name: 
  namespace:
  labels:
    app:
spec:
  selector:
    app: 
  ports:
    - protocol: TCP
      port: 
      targetPort:
  type: ClusterIP

